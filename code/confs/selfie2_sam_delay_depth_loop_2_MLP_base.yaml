hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: selfie2_sam_delay_depth_loop_2_MLP_model
  - dataset: selfie2_sam_delay_depth_loop_2_MLP # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}