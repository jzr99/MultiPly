hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: taichi03_sam_delay_depth20_loop_2_0pose_MLP_model
  - dataset: taichi03_sam_delay_depth20_loop_2_0pose_MLP # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}