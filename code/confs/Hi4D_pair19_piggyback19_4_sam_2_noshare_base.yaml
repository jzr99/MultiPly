hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: Hi4D_pair19_piggyback19_4_sam_2_noshare_model
  - dataset: Hi4D_pair19_piggyback19_4_sam_2_noshare # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}