hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: Hi4D_pair16_jump16_4_sam_depth_loop_model
  - dataset: Hi4D_pair16_jump16_4_sam_depth_loop # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}