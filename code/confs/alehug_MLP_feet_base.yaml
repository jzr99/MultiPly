hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: alehug_MLP_feet_model
  - dataset: alehug_MLP_feet # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}