hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: dance5_sam_delay_depth_loop_multi_add_model
  - dataset: dance5_sam_delay_depth_loop_multi_add # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}