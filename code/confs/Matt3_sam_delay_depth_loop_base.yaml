hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: Matt3_sam_delay_depth_loop_model
  - dataset: Matt3_sam_delay_depth_loop # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}