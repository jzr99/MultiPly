hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: party_dance_personid_triplane_model
  - dataset: party_dance_depth_samGT_personid_triplane # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}