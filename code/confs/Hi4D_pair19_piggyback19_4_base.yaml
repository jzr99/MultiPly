hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: Hi4D_pair19_piggyback19_4_model
  - dataset: Hi4D_pair19_piggyback19_4 # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}