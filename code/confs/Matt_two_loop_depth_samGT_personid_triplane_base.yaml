hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: Matt_two_personid_triplane_model
  - dataset: Matt_two_raw_openpose_depth_samGT_personid_triplane # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}