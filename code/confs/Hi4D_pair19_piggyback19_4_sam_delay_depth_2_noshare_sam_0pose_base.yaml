hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: Hi4D_pair19_piggyback19_4_sam_delay_depth_2_noshare_sam_0pose_model
  - dataset: Hi4D_pair19_piggyback19_4_sam_delay_depth_2_noshare_sam_0pose # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}