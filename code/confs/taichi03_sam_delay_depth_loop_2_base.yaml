hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: taichi03_sam_delay_depth_loop_2_model
  - dataset: taichi03_sam_delay_depth_loop_2 # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}