hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: cycle_dance_model
  - dataset: cycle_dance_SAM # Hi4D_pair19_piggyback19_cam4_raw_openpose # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}