hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: updown_sam_delay_depth_loop_noshare_vitpose_openpose_1024_model
  - dataset: updown_sam_delay_depth_loop_noshare_vitpose_openpose_1024 # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}