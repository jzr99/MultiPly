hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: sydney_sam_delay_depth_loop_noshare_model
  - dataset: sydney_sam_delay_depth_loop_noshare # hi4d # video # 3DPW
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}