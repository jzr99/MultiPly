hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: volsdf_bg
  - dataset: threedpw
  - _self_

seed: 42
project_name: "VOLSDF_cam_norm"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}