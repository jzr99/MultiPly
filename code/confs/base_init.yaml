hydra:
    run:
        dir: "outputs/${exp}/${run}"

defaults:
  - model: init
  - dataset: poseprior
  - _self_

seed: 42
project_name: "SNARF-IDR-Init"
exp: ${dataset.train.dataset}
run: ${dataset.train.subject}